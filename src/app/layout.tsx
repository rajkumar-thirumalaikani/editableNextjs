import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Create and Manage Reference Data in Power BI with EDITable",
  description: "Manage your master / reference / meta data for your reporting & planning applications. This self-service product complements your Master Data Management (MDM) solution. Requires minimal setup and help from IT. Available for Power BI and supports data residing in Azure SQL, Microsoft Fabric, Data Bricks, Snowflake, Postgres and more.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
